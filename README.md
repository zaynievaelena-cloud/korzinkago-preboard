# Korzinka Go Preboarding Landing — clean GitHub Pages version

This version removes injected Kaspersky scripts and external font dependencies.

Upload all files from this folder to the repository root, then publish via Settings → Pages → main → /(root).
